# Lab Notebooks

Place your notebooks and supporting files here. When the container runs,
JupyterLab serves this directory as the root at `/app/lab_notebooks`.

Example: create `example.ipynb` inside this folder and open JupyterLab at
`http://localhost:8888` (default docker-compose mapping).
